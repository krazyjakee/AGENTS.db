[package]
name = "agentsdb-core"
version = "0.1.8"
edition = "2021"
license = "MIT"

[lints]
workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }
thiserror = "2.0"

[features]
serde = ["dep:serde"]
